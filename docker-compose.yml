version: '3.1'

services:
  db:
    image: postgres
    environment:
      POSTGRES_USER: postgres
      #POSTGRES_PASSWORD:
    ports:
      - "5432:5432"
    volumes:
      - db-extranet:/var/lib/postgresql/data

volumes:
  db-extranet: